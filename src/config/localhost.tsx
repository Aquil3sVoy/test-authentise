/**
 * Backend Url or base url of the server
 */

const backendUrl = `https://dog.ceo/api/`;
/**
 * Frontend Url. Use this in production.
 */
const frontendUrl = {
  host: "localhost:3000",
  protocol: "http",
};

export default { frontendUrl, backendUrl };
